# Python_Classification_CNN-MLP
This repository presents a comparison between a CNN and MLP for image classification on the CIFAR-10 dataset, where both models were trained under the same preprocessing and evaluation setup; the experiment shows that the CNN achieves around 78% accuracy, significantly outperforming the MLP which reaches about 48%. This is 2025 Project
